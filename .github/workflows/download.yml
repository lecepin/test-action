name: Download all file
on:
  push:
    branches:
      - main
jobs:
  build-and-download:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: get
        run: |
          npm i 
          node main.js
          
          
  
